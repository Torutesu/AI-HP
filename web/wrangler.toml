# Cloudflare Pages configuration.
# Lets both the dashboard Git integration and `wrangler pages deploy` pick up
# the build output + Functions without manual setup. The project's root
# directory on Cloudflare must be set to `web` (this file lives there).

name = "ai-hp"
compatibility_date = "2025-06-01"
pages_build_output_dir = "out"
